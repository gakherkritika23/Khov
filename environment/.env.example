# Copy this file to .env.dev, .env.uat, or .env.prod and fill in real values.
# NEVER commit the actual .env.* files — they are gitignored.

ENV=dev
BASE_URL=https://www.khov.com/
BROWSER=chromium
TEST_TIMEOUT=60000
