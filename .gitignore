# Dependencies
node_modules/

# Playwright test output — generated at runtime, must NOT be committed.
results.xml
playwright-report/
test-results/
allure-results/
allure-report/
client-report/index.html

# Email body generated by CI
email-body.html

# Environment files (contain secrets / URLs)
environment/.env.dev
environment/.env.uat
environment/.env.prod

# TypeScript build output
dist/
*.js.map

# OS / editor
.DS_Store
Thumbs.db
.vscode/settings.json
# logs
debug.log

# Generated coverage summary artifacts (local only — never commit)
docs/coverage-summary.md
docs/coverage-summary.pdf
scripts/coverage-summary.html

# AI context export bundle (local only — never commit)
khov-automation-ai-context/
khov-automation-ai-context.zip

# Claude Code runtime
.claude/scheduled_tasks.lock